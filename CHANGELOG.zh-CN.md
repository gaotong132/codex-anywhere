# 更新日志

[English](CHANGELOG.md) | 简体中文

## 未发布

- 修复父容器的汇总标签吞掉内部独立菜单选项的问题：保留文本不同的可点击子控件，同时继续去重装饰文字与图标。

- 新增实验性页面截图：当前任务已授权页默认支持视口截图，无额外设置开关，升级时清理旧截图开关。支持后台页签与可见 canvas，输入/嵌入/可检测私密区域在插件内遮挡后传输；JPEG 最大边长 1920、最多 1 MiB。页面变化或撤销时丢弃结果，MCP 原生图片返回不占用文字回包预算。需更新 Connector/MCP、重载扩展并发现新工具，无需重新配对。

- 控件点击改用浏览器鼠标事件，支持依赖指针事件、且没有残留用户激活的脚本开页。新增必需的 `debugger` 权限，更新扩展时按需确认 Chrome 的新增权限提示；事件间重新校验授权文档和可见引用，操作后断开调试连接，中断时报告不确定结果且不重放。普通受控链接和下拉选项读取保留既有流程。

- 扩展和 MCP 支持横向及双向滚动，快照标注面板方向与位置，滚动结果返回到达边界后的实际位移；点击部分露出或换行控件时使用可见片段，仍拒绝穿透真实覆盖层。需同步更新连接器/MCP 并重载扩展。

- Web 图片发送显示准备、按实际字节计算的发送进度及等待确认状态。普通请求等待时间放宽到 60 秒，图片上传/读取放宽到 120 秒，超时提示显示实际时限；保留原有发送等待时间及单项请求的显式设置。

- 修复页面滚动后因视口溢出传播、无盒容器导致的内容漏读，返回视口信息；新页解析完成即可操作，不再等待慢图片等资源。通过浏览器导航事件识别当前点击产生的单个脚本子页，保留同源、文档、站点权限校验，目标不明确时交给用户授权；需重载扩展，并按 Chrome 提示接受新增的 `webNavigation` 权限。

- 重复链接复用未编辑的受控子页，普通子页保留最近使用的 5 个并自动清理旧页；保留起始页、手动/受保护页签、输入记录及正在执行的操作。

- 点击/填写时正常聚焦控件，快照最多输出 200 个节点以覆盖密集列表；保留扫描与文本上限，并增加明确的序列化响应预算。

- 插件打开链接后，Chrome 在原窗口自动切到新页签，侧栏保留当前聊天会话；站点授权规则不变。

- 没有 `href` 的 a 元素按普通按钮点击；对它调用 `open_link` 明确返回需要真实链接，避免 URL 解析异常落入含糊错误。

- 单页应用切换 hash/history 路由时核对原 documentId 与来源，通过后保留同一文档授权；刷新、文档替换及跨域仍撤销，避免普通页签切换反复要求授权。

- 精简插件侧栏：只保留授权主操作，其余设置、撤销与重新加载收进更多菜单；仅在当前页属于当前会话时显示简洁的已授权状态。

- 控件内仅继承手形光标的装饰节点（包括空 SVG 图标节点）不再单独占用快照 ref 和节点额度。

- HTML 模板统一使用 LF 换行，避免 Windows 与 Linux checkout 的扩展构建指纹因换行差异不一致。

- Web 实时消息和历史记录支持 `request_user_input_async` 问题卡片、选项与自由输入，保留桌面端问题编号和已回答状态；答案回到原任务，聊天草稿不受影响。

- 快照去除控件内重复的标签文字，避免导航挤占正文额度；识别可聚焦或手形光标的自定义页签，保留链接纳管及禁用/遮挡检查。

- 支持在聊天输入框和新建会话首条消息中按 Ctrl/⌘+V 粘贴截图，预览后随消息上传；复用图片校验与压缩，保留普通文字粘贴。

- 修复扩展读取复杂控制台时被大量隐藏菜单提前耗尽扫描额度的问题：遍历时跳过隐藏/私有分支，保留原有读取上限，
  并返回具体截断原因，便于区分插件扫描限制与网页实际缺少控件。

- 修复大型会话在压缩记录、工具输出之后显示空白的问题：历史分页与实时快照会在有界范围内回溯到可展示消息，
  空页保留继续加载入口，并且仅在请求进行中显示加载提示。

- 补齐实验性浏览器插件的控制台表单操作：嵌套按钮文字、控件状态、数字输入、原生下拉框标签与选择、指定面板滚动。
  扩展到 MCP 保留可恢复错误的具体原因；页面列表区分当前会话无授权与页面离线，并标明连接器环境，避免误判为需要重新配对。
  需同步更新连接器/MCP，并重新加载扩展。

- 实验性浏览器插件改为聊天配对一次、页面控制自动关联和重连，无需第二条配对链接。私钥各自保存，
  关联证明绑定本次连接、扩展和设备；保留显式网页授权、旧身份及撤销边界。

- 拆分 Web 下载/预览、Markdown 渲染及 Relay HTTP 职责；修复空文件、提前结束的下载、会话切换取消及屏幕锁归属。
  校验预览字节数并保留 UTF-8 BOM，隔离扩展旧连接回调，区分双向 RPC 请求 ID，并修正日志改写后的历史缓存。
  范围与验证记录见[架构审查](docs/refactoring-2026-09.md)。

- 支持 SVG 文件、本地 SVG 图片引用和 `svg` 代码块的矢量图预览，保留源码查看和原有下载入口；
  使用隔离的图片上下文渲染，并补全 Linux 本地文件链接及 Markdown 相对路径解析。

- 浏览器按用户任务直接执行，区分 MCP 宿主审批拒绝与网页/登录失败，补充 `never` 策略下逐工具预授权配置和真实写操作探针。
  新站点先打开供用户授权但不读取，普通链接保留原页授权；旧版内部指引继续从历史中隐藏。

- `relay.sh devices` 新增在线/离线/未知状态、连接数、最近连接与最后在线时间，支持 `--json`。
  私有活跃记录与设备信任状态分开保存，重启保留历史，快照过期时显示未知。

- 修复旧页签关闭、插件丢失本地记录后，连接器残留起始页授权导致无法再次授权的问题。显式授权可替换本插件的
  旧授权或完全离线的旧浏览器占位，级联撤销子页并取消在途操作；阻止自动恢复及迟到请求夺回新授权。

- 修复侧栏切换标签页后无法授权普通网页的问题：读取当前标签页地址，并在授权点击时申请当前站点访问。
  页面控制设置移除重复的环境/会话选择，直接使用聊天当前会话；权限提示期间切页、刷新或切会话会取消本次授权。

- 实验性浏览器插件新增侧栏聊天，复用线上 Web。侧栏同步当前会话并提供显式页面授权，保留独立配对和后台授权生命周期；
  新增受扩展白名单限制的嵌入入口、消息来源及新鲜度校验，以及跨窗口/文档替换回归。

- 通过所选连接器的动态模型目录支持 GPT-6 Astra；展示宿主提供的全部推理档位，区分 Max 与 Ultra，编辑和保存时保留原档位。
- 浏览器内部指引不再混入用户消息展示或历史匹配；Desktop 连接器释放运行时前等待在途 RPC，避免中断并发读取。
- 将本机只读原型替换为可配置的扩展配对、执行环境/已有 Session 选择和文档授权，增加通过端到端加密路由、
  私有本机 IPC 调用的会话绑定 MCP 快照/点击/输入/滚动工具。取消十分钟授权限制，不新建替代会话、不接管 Desktop 写入端。
- 改为紧凑面板、固定图标与标签页状态点、次级撤销菜单，并在 Web 显示 Session 浏览器状态。
  新增 SDK/IPC、构建后 worker 真 WS/E2E 回归与真实临时 Codex 探针；真实 Chrome/Edge、Desktop UI 和 ECS
  仍需验收，不自动部署现网或发布版本。

## v0.2.1 — 2026-09-04

- 统一按钮、链接、菜单和折叠控件的点击反馈：去除浏览器默认触摸闪块，hover 样式只作用于支持悬停的
  指针，保留选中颜色和键盘焦点提示。触摸滚动菜单不再误切换高亮项，思考强度滑块不再出现文本框焦点光晕。
- 优化正文后附 heartbeat 控制块的自动任务消息：完整保留报告及后续补充文字，用简洁来源标识替代
  内部字段和重复摘要，复制时也只包含正文；覆盖转义格式与转发历史，不误删 Markdown 代码示例。
- 修复浏览器配对链接未自动填码的问题，同时支持已打开页面接收新链接。配对时保留取消入口，
  超过 15 秒自动超时，失败后恢复输入且不再自动重试；旧连接和已取消的二维码回调不会干扰新尝试。
- 修复 Desktop 会话隔离缺陷：手机输入不再借用其他会话作为来源，避免修复汇报被回发到无关任务。
  会话级原生调用绑定来源与目标，拒绝身份不匹配，失败时不会回退到其他会话。更新转发服务后还必须
  重新构建并重启 Windows 连接器；此修复不改写既有历史。

## v0.2.0 — 2026-09-04

- 新增相互隔离的执行环境：既可连接 Windows/Desktop，也可连接 24×7 运行的 Linux/ECS 无头节点；
  会话、工作目录、未读状态和文件都按环境隔离。
- 新增会话重命名，以及按会话设置模型、轻度/中/高/极高四档思考强度、快速模式和 Web 权限模式；
  Desktop 已持有的审批仍留在电脑上处理。
- 补全实时与历史诊断：展示定时任务提示词、渐变上下文用量、压缩节点、运行详情、连接器持有轮次停止，
  以及按轮次隔离的有界 Diff。
- 长会话改为明确的增量历史：加载状态可见、失败可重试、滚动位置稳定，启动时不再递归拉取旧页面。
- 将浏览器请求管理、会话配置、app-server 历史与权限、工作区策略和实时活动拆分为聚焦模块，并增加
  对应回归测试。
- Desktop 活动状态改为异步合并并去重并发刷新，Desktop 桥响应慢或不可用时不会再阻塞会话列表和启动。

## v0.1.5 — 2026-09-03

- 新增手机端上下文用量与轮次诊断，以及按轮次隔离的有界变更查看。
