# 更新日志

[English](CHANGELOG.md) | 简体中文

## 未发布

暂无。

## v0.3.0 — 2026-09-12

- 新增实验性 Browser Agent：侧栏沿用当前任务，Web 配对一次后关联插件独立身份，页面仍需明确授权；八个 MCP 工具覆盖列页、快照、截图、缩放、点击、输入、滚动及打开链接。

- AI 打开的页签在原窗口跟随，复用符合条件的子页，普通子页保留最近使用的 5 个。同源导航保留授权，跨源目标需要授权；仅在明确授权时回收失联的旧页面占位。

- 支持原生鼠标控件、数字输入、原生下拉标签及面板横纵滚动。新增带私密区域遮挡的有界视口截图和 50%–200% 独立页签缩放；横向裁剪优先使用 80%、67%，每次刷新快照引用。

- 聊天与新任务支持粘贴 PNG/JPEG/WebP 截图，显示图片准备、按字节计算的发送进度和接收确认，图片上传/读取超时为 120 秒。

- Web 支持通过选项或自由文本回答 request_user_input_async，保留原任务、输入草稿、问题 ID，并同步 Desktop 回答；已回答卡片和重复问题引用折叠显示。

- 生成图片兼容旧格式和新版 Codex Extension 事件，补全摘要 RPC 遗漏的图片，恢复大图片记录并避免翻页重复。仅传图片引用和校验后的预览，不传 Base64 工具历史。

- 支持所选 Connector 声明的 GPT-6 Astra 与全部思考档位，包括 Max、Ultra。

- 上下文压缩中与完成状态共用一个时间线标记；显示终止错误原因，跨大块隐藏记录恢复历史，并保留稳定消息身份。

- 浏览器提示缩为 91 字符，仅首次或页面/在线状态变化时发送；通用规则保留在 MCP 说明中，历史中的旧提示继续隐藏。

- 支持 SVG 预览，文本/代码/Markdown 预览允许可读的受支持绝对路径；位图根目录与确认下载分别管理。修复空文件、截断下载、取消及屏幕常亮锁归属。

- relay.sh devices 与 --json 可查看已批准端点的连接数和最近连接/在线时间，设备信任与活动记录分开保存；加固 Desktop 任务隔离、RPC 排空和插件生命周期。

- Docker 显式设置包元数据权限，避免严格目录权限导致非 root Node 启动失败。刷新全部指南、统一版本信息，并补充固定 tag 升级步骤。

参见 [v0.3.0 升级步骤与限制](docs/release-0.3.0.zh-CN.md)。协调更新 Relay/Web、全部 Connector 和可选插件，保留现有配对身份。

## v0.2.1 — 2026-09-04

- 统一按钮、链接、菜单和折叠控件的点击反馈：去除浏览器默认触摸闪块，hover 样式只作用于支持悬停的
  指针，保留选中颜色和键盘焦点提示。触摸滚动菜单不再误切换高亮项，思考强度滑块不再出现文本框焦点光晕。
- 优化正文后附 heartbeat 控制块的自动任务消息：完整保留报告及后续补充文字，用简洁来源标识替代
  内部字段和重复摘要，复制时也只包含正文；覆盖转义格式与转发历史，不误删 Markdown 代码示例。
- 修复浏览器配对链接未自动填码的问题，同时支持已打开页面接收新链接。配对时保留取消入口，
  超过 15 秒自动超时，失败后恢复输入且不再自动重试；旧连接和已取消的二维码回调不会干扰新尝试。
- 修复 Desktop 会话隔离缺陷：手机输入不再借用其他会话作为来源，避免修复汇报被回发到无关任务。
  会话级原生调用绑定来源与目标，拒绝身份不匹配，失败时不会回退到其他会话。更新转发服务后还必须
  重新构建并重启 Windows 连接器；此修复不改写既有历史。

## v0.2.0 — 2026-09-04

- 新增相互隔离的执行环境：既可连接 Windows/Desktop，也可连接 24×7 运行的 Linux/ECS 无头节点；
  会话、工作目录、未读状态和文件都按环境隔离。
- 新增会话重命名，以及按会话设置模型、轻度/中/高/极高四档思考强度、快速模式和 Web 权限模式；
  Desktop 已持有的审批仍留在电脑上处理。
- 补全实时与历史诊断：展示定时任务提示词、渐变上下文用量、压缩节点、运行详情、连接器持有轮次停止，
  以及按轮次隔离的有界 Diff。
- 长会话改为明确的增量历史：加载状态可见、失败可重试、滚动位置稳定，启动时不再递归拉取旧页面。
- 将浏览器请求管理、会话配置、app-server 历史与权限、工作区策略和实时活动拆分为聚焦模块，并增加
  对应回归测试。
- Desktop 活动状态改为异步合并并去重并发刷新，Desktop 桥响应慢或不可用时不会再阻塞会话列表和启动。

## v0.1.5 — 2026-09-03

- 新增手机端上下文用量与轮次诊断，以及按轮次隔离的有界变更查看。
